post:
  tags:
  - campaign
  description: create campaign api
  operationId: campaign/create
  security:
    - api-token: []


  responses:
    200:
      $ref: '../../schema/success_response.yml'
    401:
      $ref: '../../schema/unauthorized_response.yml'

  requestBody:
    $ref: '../../schema/campaign_request.yml'